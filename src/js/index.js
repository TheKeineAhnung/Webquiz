/*function correctWindowDivHeight() {
  var div = document.getElementById("menu-iframe").srcdoc;
  console.log(document.getElementsByTagName("div"));
  console.log(div);
  var iframe = document.getElementById("menu-iframe");
  var iframeHeight = getComputedStyle(iframe).height;
  div.style.height = iframeHeight;
  console.log(div.style.height);
}

window.onload = correctWindowDivHeight();
window.addEventListener("resize", correctWindowDivHeight);*/
